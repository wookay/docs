var documenterSearchIndex = {"docs":
[{"category":"section","location":"essentials/","page":"Essentials","text":"note: Note\ndoc fromjulia/base/essentials.jl\njulia/base/expr.jl\njulia/Compiler/src/effects.jl\n\nPages = [\"essentials.md\"]\nDepth = 2:2\n\nPages = [\"essentials.md\"]","title":"Essentials"},{"category":"macro","location":"essentials/#Base.@_total_meta","page":"Essentials","text":"can be used in place of @assume_effects :total (supposed to be used for bootstrapping)\n\n:total\n\nThis setting is the maximum possible set of effects. It currently implies the following other settings:\n\n:consistent\n:effect_free\n:nothrow\n:terminates_globally\n:notaskstate\n:inaccessiblememonly\n:noub\n:nortcall\n\nwarning: Warning\n:total is a very strong assertion and will likely gain additional semantics in future versions of Julia (e.g. if additional effects are added and included in the definition of :total). As a result, it should be used with care. Whenever possible, prefer to use the minimum possible set of specific effect assertions required for a particular application. In cases where a large number of effect overrides apply to a set of functions, a custom macro is recommended over the use of :total.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_total_meta"},{"category":"macro","location":"essentials/#Base.@_terminates_globally_meta","page":"Essentials","text":"can be used in place of @assume_effects :terminates_globally (supposed to be used for bootstrapping)\n\n:terminates_globally\n\nThe :terminates_globally setting asserts that this method will eventually terminate (either normally or abnormally), i.e. does not loop indefinitely.\n\nnote: Note\nThis :terminates_globally assertion covers any other methods called by the annotated method.\n\nnote: Note\nThe compiler will consider this a strong indication that the method will terminate relatively quickly and may (if otherwise legal) call this method at compile time. I.e. it is a bad idea to annotate this setting on a method that technically, but not practically, terminates.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_terminates_globally_meta"},{"category":"macro","location":"essentials/#Base.@_terminates_globally_noub_meta","page":"Essentials","text":"can be used in place of @assume_effects :terminates_globally :noub (supposed to be used for bootstrapping)\n\n:terminates_globally\n\nThe :terminates_globally setting asserts that this method will eventually terminate (either normally or abnormally), i.e. does not loop indefinitely.\n\nnote: Note\nThis :terminates_globally assertion covers any other methods called by the annotated method.\n\nnote: Note\nThe compiler will consider this a strong indication that the method will terminate relatively quickly and may (if otherwise legal) call this method at compile time. I.e. it is a bad idea to annotate this setting on a method that technically, but not practically, terminates.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n:noub\n\nThe :noub setting asserts that the method will not execute any undefined behavior (for any input). Note that undefined behavior may technically cause the method to violate any other effect assertions (such as :consistent or :effect_free) as well, but we do not model this, and they assume the absence of undefined behavior.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_terminates_globally_noub_meta"},{"category":"macro","location":"essentials/#Base.@_foldable_meta","page":"Essentials","text":"can be used in place of @assume_effects :foldable (supposed to be used for bootstrapping)\n\n:foldable\n\nThis setting is a convenient shortcut for the set of effects that the compiler requires to be guaranteed to constant fold a call at compile time. It is currently equivalent to the following settings:\n\n:consistent\n:effect_free\n:terminates_globally\n:noub\n:nortcall\n\nnote: Note\nThis list in particular does not include :nothrow. The compiler will still attempt constant propagation and note any thrown error at compile time. Note however, that by the :consistent-cy requirements, any such annotated call must consistently throw given the same argument values.\n\nnote: Note\nAn explicit @inbounds annotation inside the function will also disable constant folding and not be overridden by :foldable.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_foldable_meta"},{"category":"macro","location":"essentials/#Base.@_nothrow_noub_meta","page":"Essentials","text":"can be used in place of @assume_effects :nothrow :noub (supposed to be used for bootstrapping)\n\n:nothrow\n\nThe :nothrow setting asserts that this method does not throw an exception (i.e. will either always return a value or never return).\n\nnote: Note\nIt is permissible for :nothrow annotated methods to make use of exception handling internally as long as the exception is not rethrown out of the method itself.\n\nnote: Note\nIf the execution of a method may raise MethodErrors and similar exceptions, then the method is not considered as :nothrow. However, note that environment-dependent errors like StackOverflowError or InterruptException are not modeled by this effect and thus a method that may result in StackOverflowError does not necessarily need to be !:nothrow (although it should usually be !:terminates too).– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n:noub\n\nThe :noub setting asserts that the method will not execute any undefined behavior (for any input). Note that undefined behavior may technically cause the method to violate any other effect assertions (such as :consistent or :effect_free) as well, but we do not model this, and they assume the absence of undefined behavior.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_nothrow_noub_meta"},{"category":"macro","location":"essentials/#Base.@_terminates_locally_meta","page":"Essentials","text":"can be used in place of @assume_effects :terminates_locally (supposed to be used for bootstrapping)\n\n:terminates_locally\n\nThe :terminates_locally setting is like :terminates_globally, except that it only applies to syntactic control flow within the annotated method. It is thus a much weaker (and thus safer) assertion that allows for the possibility of non-termination if the method calls some other method that does not terminate.\n\nnote: Note\n:terminates_globally implies :terminates_locally.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_terminates_locally_meta"},{"category":"macro","location":"essentials/#Base.@_terminates_globally_notaskstate_meta","page":"Essentials","text":"can be used in place of @assume_effects :terminates_globally :notaskstate (supposed to be used for bootstrapping)\n\n:terminates_globally\n\nThe :terminates_globally setting asserts that this method will eventually terminate (either normally or abnormally), i.e. does not loop indefinitely.\n\nnote: Note\nThis :terminates_globally assertion covers any other methods called by the annotated method.\n\nnote: Note\nThe compiler will consider this a strong indication that the method will terminate relatively quickly and may (if otherwise legal) call this method at compile time. I.e. it is a bad idea to annotate this setting on a method that technically, but not practically, terminates.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n:notaskstate\n\nThe :notaskstate setting asserts that the method does not use or modify the local task state (task local storage, RNG state, etc.) and may thus be safely moved between tasks without observable results.\n\nnote: Note\nThe implementation of exception handling makes use of state stored in the task object. However, this state is currently not considered to be within the scope of :notaskstate and is tracked separately using the :nothrow effect.\n\nnote: Note\nThe :notaskstate assertion concerns the state of the currently running task. If a reference to a Task object is obtained by some other means that does not consider which task is currently running, the :notaskstate effect need not be tainted. This is true, even if said task object happens to be === to the currently running task.\n\nnote: Note\nAccess to task state usually also results in the tainting of other effects, such as :effect_free (if task state is modified) or :consistent (if task state is used in the computation of the result). In particular, code that is not :notaskstate, but is :effect_free and :consistent may still be dead-code-eliminated and thus promoted to :total.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_terminates_globally_notaskstate_meta"},{"category":"macro","location":"essentials/#Base.@_nospecializeinfer_meta","page":"Essentials","text":"Base.@nospecializeinfer function f(args...)\n    @nospecialize ...\n    ...\nend\nBase.@nospecializeinfer f(@nospecialize args...) = ...\n\nTells the compiler to infer f using the declared types of @nospecialized arguments. This can be used to limit the number of compiler-generated specializations during inference.\n\nExamples\n\njulia> f(A::AbstractArray) = g(A)\nf (generic function with 1 method)\n\njulia> @noinline Base.@nospecializeinfer g(@nospecialize(A::AbstractArray)) = A[1]\ng (generic function with 1 method)\n\njulia> @code_typed f([1.0])\nCodeInfo(\n1 ─ %1 = invoke Main.g(_2::AbstractArray)::Any\n└──      return %1\n) => Any\n\nIn this example, f will be inferred for each specific type of A, but g will only be inferred once with the declared argument type A::AbstractArray, meaning that the compiler will not likely see the excessive inference time on it while it can not infer the concrete return type of it. Without the @nospecializeinfer, f([1.0]) would infer the return type of g as Float64, indicating that inference ran for g(::Vector{Float64}) despite the prohibition on specialized code generation.\n\ncompat: Julia 1.10\nUsing Base.@nospecializeinfer requires Julia version 1.10.\n\n\n\n\n\n","title":"Base.@_nospecializeinfer_meta"},{"category":"macro","location":"essentials/#Base.@_nothrow_meta","page":"Essentials","text":"can be used in place of @assume_effects :nothrow (supposed to be used for bootstrapping)\n\n:nothrow\n\nThe :nothrow setting asserts that this method does not throw an exception (i.e. will either always return a value or never return).\n\nnote: Note\nIt is permissible for :nothrow annotated methods to make use of exception handling internally as long as the exception is not rethrown out of the method itself.\n\nnote: Note\nIf the execution of a method may raise MethodErrors and similar exceptions, then the method is not considered as :nothrow. However, note that environment-dependent errors like StackOverflowError or InterruptException are not modeled by this effect and thus a method that may result in StackOverflowError does not necessarily need to be !:nothrow (although it should usually be !:terminates too).– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_nothrow_meta"},{"category":"macro","location":"essentials/#Base.@_effect_free_terminates_locally_meta","page":"Essentials","text":"can be used in place of @assume_effects :effect_free :terminates_locally (supposed to be used for bootstrapping)\n\n:effect_free\n\nThe :effect_free setting asserts that the method is free of externally semantically visible side effects. The following is an incomplete list of externally semantically visible side effects:\n\nChanging the value of a global variable.\nMutating the heap (e.g. an array or mutable value), except as noted below\nChanging the method table (e.g. through calls to eval)\nFile/Network/etc. I/O\nTask switching\n\nHowever, the following are explicitly not semantically visible, even if they may be observable:\n\nMemory allocations (both mutable and immutable)\nElapsed time\nGarbage collection\nHeap mutations of objects whose lifetime does not exceed the method (i.e. were allocated in the method and do not escape).\nThe returned value (which is externally visible, but not a side effect)\n\nThe rule of thumb here is that an externally visible side effect is anything that would affect the execution of the remainder of the program if the function were not executed.\n\nnote: Note\nThe :effect_free assertion is made both for the method itself and any code that is executed by the method. Keep in mind that the assertion must be valid for all world ages and limit use of this assertion accordingly.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n:terminates_locally\n\nThe :terminates_locally setting is like :terminates_globally, except that it only applies to syntactic control flow within the annotated method. It is thus a much weaker (and thus safer) assertion that allows for the possibility of non-termination if the method calls some other method that does not terminate.\n\nnote: Note\n:terminates_globally implies :terminates_locally.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_effect_free_terminates_locally_meta"},{"category":"macro","location":"essentials/#Base.@_noub_if_noinbounds_meta","page":"Essentials","text":"can be used in place of @assume_effects :noub_if_noinbounds (supposed to be used for bootstrapping)\nnoub::UInt8:\nALWAYS_TRUE: this method is guaranteed to not execute any undefined behavior (for any input).\nALWAYS_FALSE: this method may execute undefined behavior.\nNOUB_IF_NOINBOUNDS: this method is guaranteed to not execute any undefined behavior under the assumption that its @boundscheck code is not elided (which happens when the caller does not set nor propagate the @inbounds context)\nNote that undefined behavior may technically cause the method to violate any other effect assertions (such as :consistent or :effect_free) as well, but we do not model this, and they assume the absence of undefined behavior.– doc from julia/Compiler/src/effects.jl    struct Effects\n\n\n\n\n\n","title":"Base.@_noub_if_noinbounds_meta"},{"category":"macro","location":"essentials/#Base.@_propagate_inbounds_meta","page":"Essentials","text":"another version of inlining that propagates an inbounds context   @propagate_inbounds\n\nTells the compiler to inline a function while retaining the caller's inbounds context.\n\n\n\n\n\n","title":"Base.@_propagate_inbounds_meta"},{"category":"macro","location":"essentials/#Base.@_noub_meta","page":"Essentials","text":"can be used in place of @assume_effects :noub (supposed to be used for bootstrapping)\n\n:noub\n\nThe :noub setting asserts that the method will not execute any undefined behavior (for any input). Note that undefined behavior may technically cause the method to violate any other effect assertions (such as :consistent or :effect_free) as well, but we do not model this, and they assume the absence of undefined behavior.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_noub_meta"},{"category":"macro","location":"essentials/#Base.@_notaskstate_meta","page":"Essentials","text":"can be used in place of @assume_effects :notaskstate (supposed to be used for bootstrapping)\n\n:notaskstate\n\nThe :notaskstate setting asserts that the method does not use or modify the local task state (task local storage, RNG state, etc.) and may thus be safely moved between tasks without observable results.\n\nnote: Note\nThe implementation of exception handling makes use of state stored in the task object. However, this state is currently not considered to be within the scope of :notaskstate and is tracked separately using the :nothrow effect.\n\nnote: Note\nThe :notaskstate assertion concerns the state of the currently running task. If a reference to a Task object is obtained by some other means that does not consider which task is currently running, the :notaskstate effect need not be tainted. This is true, even if said task object happens to be === to the currently running task.\n\nnote: Note\nAccess to task state usually also results in the tainting of other effects, such as :effect_free (if task state is modified) or :consistent (if task state is used in the computation of the result). In particular, code that is not :notaskstate, but is :effect_free and :consistent may still be dead-code-eliminated and thus promoted to :total.– doc from julia/base/expr.jl    macro assume_effects(args...)\n\n\n\n\n\n","title":"Base.@_notaskstate_meta"},{"category":"section","location":"effects/#Effects","page":"Effects","text":"julia> using ButterEffects\n\nhelp?> +c\n### Effects: +c\n...","title":"Effects"},{"category":"section","location":"effects/#effectbits-letters","page":"Effects","text":"+c   -c   ?c\n+e   -e   ?e\n+re  -re  ?re\n+n   -n\n+t   -t\n+s   -s\n+m   -m   ?m\n+u   -u   ?u\n+o   -o   ?o\n+r   -r","title":"effectbits letters"},{"category":"section","location":"effects/#effectbits-suffixes","page":"Effects","text":"c  e  re  n  t  s  m  u  o  r","title":"effectbits suffixes"},{"category":"section","location":"effects/#effect-settings","page":"Effects","text":":consistent\n:effect_free\n:reset_safe\n:nothrow\n:terminates_globally\n:terminates_locally\n:notaskstate\n:inaccessiblememonly\n:noub\n:noub_if_noinbounds\n:nonoverlayed\n:consistent_overlay\n:nortcall\n:foldable\n:removable\n:total","title":"effect settings"},{"category":"section","location":"effects/#effectbits-consts","page":"Effects","text":"CONSISTENT_IF_NOTRETURNED\nCONSISTENT_IF_INACCESSIBLEMEMONLY\nEFFECT_FREE_IF_INACCESSIBLEMEMONLY\nRESET_SAFE_IF_INACCESSIBLEMEMONLY\nINACCESSIBLEMEM_OR_ARGMEMONLY\nNOUB_IF_NOINBOUNDS\nCONSISTENT_OVERLAY","title":"effectbits consts"},{"category":"section","location":"binding_partition/#Binding-Partition","page":"Binding Partition","text":"julia> using ButterEffects\n\nhelp?> PARTITION_KIND_CONST\nconst PARTITION_KIND_CONST = 0x00  <==\n...\n\n# from julia/base/runtime_internals.jl\n#      julia/src/julia.h\n\nPARTITION_KIND_CONST\nPARTITION_KIND_CONST_IMPORT\nPARTITION_KIND_GLOBAL\nPARTITION_KIND_IMPLICIT_GLOBAL\nPARTITION_KIND_IMPLICIT_CONST\nPARTITION_KIND_EXPLICIT\nPARTITION_KIND_IMPORTED\nPARTITION_KIND_FAILED\nPARTITION_KIND_DECLARED\nPARTITION_KIND_GUARD\nPARTITION_KIND_UNDEF_CONST\nPARTITION_KIND_BACKDATED_CONST\n\nPARTITION_FLAG_EXPORTED\nPARTITION_FLAG_DEPRECATED\nPARTITION_FLAG_DEPWARN\nPARTITION_FLAG_IMPLICITLY_EXPORTED\nPARTITION_FLAG_IMPLICITLY_DEPRECATED\n\nPARTITION_MASK_KIND\nPARTITION_MASK_FLAG","title":"Binding Partition"},{"category":"section","location":"ssair/","page":"SSA IR","text":"note: Note\ndoc fromjulia/doc/src/devdocs/ssair.md\njulia/src/jltypes.c\n\nPages = [\"ssair.md\"]\nDepth = 2:2\n\nPages = [\"ssair.md\"]\nOrder = [:function, :type]","title":"SSA IR"},{"category":"type","location":"ssair/#Core.PiNode","page":"SSA IR","text":"struct PiNode <: Any\n  val::Any\n  typ::Any\nend\n\nPiNodes encode statically proven information that may be implicitly assumed in basic blocks dominated by a given pi node. They are conceptually equivalent to the technique introduced in the paper ABCD: Eliminating Array Bounds Checks on Demand or the predicate info nodes in LLVM. To see how they work, consider, e.g.\n\n%x::Union{Int, Float64} # %x is some Union{Int, Float64} typed ssa value\nif isa(x, Int)\n    # use x\nelse\n    # use x\nend\n\nWe can perform predicate insertion and turn this into:\n\n%x::Union{Int, Float64} # %x is some Union{Int, Float64} typed ssa value\nif isa(x, Int)\n    %x_int = PiNode(x, Int)\n    # use %x_int\nelse\n    %x_float = PiNode(x, Float64)\n    # use %x_float\nend\n\nPi nodes are generally ignored in the interpreter, since they don't have any effect on the values, but they may sometimes lead to code generation in the compiler (e.g. to change from an implicitly union split representation to a plain unboxed representation). The main usefulness of PiNodes stems from the fact that path conditions of the values can be accumulated simply by def-use chain walking that is generally done for most optimizations that care about these conditions anyway.\n\ndoc from julia/doc/src/devdocs/ssair.md\n\n\n\n\n\n","title":"Core.PiNode"},{"category":"type","location":"ssair/#Core.UpsilonNode","page":"SSA IR","text":"ϒ\n\nstruct UpsilonNode <: Any\n  val::Any\nend\n\nPhiC nodes and Upsilon nodes\n\nException handling complicates the SSA story moderately, because exception handling introduces additional control flow edges into the IR across which values must be tracked. One approach to do so, which is followed by LLVM, is to make calls which may throw exceptions into basic block terminators and add an explicit control flow edge to the catch handler:\n\ninvoke @function_that_may_throw() to label %regular unwind to %catch\n\nregular:\n# Control flow continues here\n\ncatch:\n# Exceptions go here\n\nHowever, this is problematic in a language like Julia, where at the start of the optimization pipeline, we do not know which calls throw. We would have to conservatively assume that every call (which in Julia is every statement) throws. This would have several negative effects. On the one hand, it would essentially reduce the scope of every basic block to a single call, defeating the purpose of having operations be performed at the basic block level. On the other hand, every catch basic block would have n*m phi node arguments (n, the number of statements in the critical region, m the number of live values through the catch block).\n\nTo work around this, we use a combination of Upsilon and PhiC nodes (the C standing for catch, written φᶜ in the IR pretty printer, because unicode subscript c is not available). There are several ways to think of these nodes, but perhaps the easiest is to think of each PhiC as a load from a unique store-many, read-once slot, with Upsilon being the corresponding store operation. The PhiC has an operand list of all the upsilon nodes that store to its implicit slot. The Upsilon nodes however, do not record which PhiC node they store to. This is done for more natural integration with the rest of the SSA IR. E.g. if there are no more uses of a PhiC node, it is safe to delete it, and the same is true of an Upsilon node. In most IR passes, PhiC nodes can be treated like Phi nodes. One can follow use-def chains through them, and they can be lifted to new PhiC nodes and new Upsilon nodes (in the same places as the original Upsilon nodes). The result of this scheme is that the number of Upsilon nodes (and PhiC arguments) is proportional to the number of assigned values to a particular variable (before SSA conversion), rather than the number of statements in the critical region.\n\nTo see this scheme in action, consider the function\n\n@noinline opaque() = invokelatest(identity, nothing) # Something opaque\nfunction foo()\n    local y\n    x = 1\n    try\n        y = 2\n        opaque()\n        y = 3\n        error()\n    catch\n    end\n    (x, y)\nend\n\nThe corresponding IR (with irrelevant types stripped) is:\n\n1 ─       nothing::Nothing\n2 ─ %2  = $(Expr(:enter, #4))\n3 ─ %3  = ϒ (false)\n│   %4  = ϒ (#undef)\n│   %5  = ϒ (1)\n│   %6  = ϒ (true)\n│   %7  = ϒ (2)\n│         invoke Main.opaque()::Any\n│   %9  = ϒ (true)\n│   %10 = ϒ (3)\n│         invoke Main.error()::Union{}\n└──       $(Expr(:unreachable))::Union{}\n4 ┄ %13 = φᶜ (%3, %6, %9)::Bool\n│   %14 = φᶜ (%4, %7, %10)::Core.Compiler.MaybeUndef(Int64)\n│   %15 = φᶜ (%5)::Core.Const(1)\n└──       $(Expr(:leave, Core.SSAValue(2)))\n5 ─       $(Expr(:pop_exception, :(%2)))::Any\n│         $(Expr(:throw_undef_if_not, :y, :(%13)))::Any\n│   %19 = Core.tuple(%15, %14)\n└──       return %19\n\nNote in particular that every value live into the critical region gets an upsilon node at the top of the critical region. This is because catch blocks are considered to have an invisible control flow edge from outside the function. As a result, no SSA value dominates the catch blocks, and all incoming values have to come through a φᶜ node.\n\ndoc from julia/doc/src/devdocs/ssair.md\n\n\n\n\n\n","title":"Core.UpsilonNode"},{"category":"type","location":"ssair/#Core.PhiCNode","page":"SSA IR","text":"φᶜ\n\nstruct PhiCNode <: Any\n  values::Vector{Any}\nend\n\nPhiC nodes and Upsilon nodes\n\nException handling complicates the SSA story moderately, because exception handling introduces additional control flow edges into the IR across which values must be tracked. One approach to do so, which is followed by LLVM, is to make calls which may throw exceptions into basic block terminators and add an explicit control flow edge to the catch handler:\n\ninvoke @function_that_may_throw() to label %regular unwind to %catch\n\nregular:\n# Control flow continues here\n\ncatch:\n# Exceptions go here\n\nHowever, this is problematic in a language like Julia, where at the start of the optimization pipeline, we do not know which calls throw. We would have to conservatively assume that every call (which in Julia is every statement) throws. This would have several negative effects. On the one hand, it would essentially reduce the scope of every basic block to a single call, defeating the purpose of having operations be performed at the basic block level. On the other hand, every catch basic block would have n*m phi node arguments (n, the number of statements in the critical region, m the number of live values through the catch block).\n\nTo work around this, we use a combination of Upsilon and PhiC nodes (the C standing for catch, written φᶜ in the IR pretty printer, because unicode subscript c is not available). There are several ways to think of these nodes, but perhaps the easiest is to think of each PhiC as a load from a unique store-many, read-once slot, with Upsilon being the corresponding store operation. The PhiC has an operand list of all the upsilon nodes that store to its implicit slot. The Upsilon nodes however, do not record which PhiC node they store to. This is done for more natural integration with the rest of the SSA IR. E.g. if there are no more uses of a PhiC node, it is safe to delete it, and the same is true of an Upsilon node. In most IR passes, PhiC nodes can be treated like Phi nodes. One can follow use-def chains through them, and they can be lifted to new PhiC nodes and new Upsilon nodes (in the same places as the original Upsilon nodes). The result of this scheme is that the number of Upsilon nodes (and PhiC arguments) is proportional to the number of assigned values to a particular variable (before SSA conversion), rather than the number of statements in the critical region.\n\nTo see this scheme in action, consider the function\n\n@noinline opaque() = invokelatest(identity, nothing) # Something opaque\nfunction foo()\n    local y\n    x = 1\n    try\n        y = 2\n        opaque()\n        y = 3\n        error()\n    catch\n    end\n    (x, y)\nend\n\nThe corresponding IR (with irrelevant types stripped) is:\n\n1 ─       nothing::Nothing\n2 ─ %2  = $(Expr(:enter, #4))\n3 ─ %3  = ϒ (false)\n│   %4  = ϒ (#undef)\n│   %5  = ϒ (1)\n│   %6  = ϒ (true)\n│   %7  = ϒ (2)\n│         invoke Main.opaque()::Any\n│   %9  = ϒ (true)\n│   %10 = ϒ (3)\n│         invoke Main.error()::Union{}\n└──       $(Expr(:unreachable))::Union{}\n4 ┄ %13 = φᶜ (%3, %6, %9)::Bool\n│   %14 = φᶜ (%4, %7, %10)::Core.Compiler.MaybeUndef(Int64)\n│   %15 = φᶜ (%5)::Core.Const(1)\n└──       $(Expr(:leave, Core.SSAValue(2)))\n5 ─       $(Expr(:pop_exception, :(%2)))::Any\n│         $(Expr(:throw_undef_if_not, :y, :(%13)))::Any\n│   %19 = Core.tuple(%15, %14)\n└──       return %19\n\nNote in particular that every value live into the critical region gets an upsilon node at the top of the critical region. This is because catch blocks are considered to have an invisible control flow edge from outside the function. As a result, no SSA value dominates the catch blocks, and all incoming values have to come through a φᶜ node.\n\ndoc from julia/doc/src/devdocs/ssair.md\n\n\n\n\n\n","title":"Core.PhiCNode"},{"category":"type","location":"ssair/#Core.PhiNode","page":"SSA IR","text":"φ\n\nThe SSA IR representation has four categories of IR nodes: Phi, Pi, PhiC, and Upsilon nodes (the latter two are only used for exception handling).\n\nPhi nodes and Pi nodes\n\nPhi nodes are part of generic SSA abstraction (see the link above if you're not familiar with the concept). In the Julia IR, these nodes are represented as:\n\nstruct PhiNode\n    edges::Vector{Int32}\n    values::Vector{Any}\nend\n\nwhere we ensure that both vectors always have the same length. In the canonical representation (the one handled by codegen and the interpreter), the edge values indicate come-from statement numbers (i.e. if edge has an entry of 15, there must be a goto, gotoifnot or implicit fall through from statement 15 that targets this phi node). Values are either SSA values or constants. It is also possible for a value to be unassigned if the variable was not defined on this path. However, undefinedness checks get explicitly inserted and represented as booleans after middle end optimizations, so code generators may assume that any use of a Phi node will have an assigned value in the corresponding slot. It is also legal for the mapping to be incomplete, i.e. for a Phi node to have missing incoming edges. In that case, it must be dynamically guaranteed that the corresponding value will not be used.\n\nNote that SSA uses semantically occur after the terminator of the corresponding predecessor (\"on the edge\"). Consequently, if multiple Phi nodes appear at the start of a basic block, they are run simultaneously. This means that in the following IR snippet, if we came from block 23, %46 will take the value associated to %45 before we entered this block.\n\n%45 = φ (#18 => %23, #23 => %50)\n%46 = φ (#18 => 1.0, #23 => %45)\n\ndoc from julia/doc/src/devdocs/ssair.md\n\n\n\n\n\n","title":"Core.PhiNode"},{"category":"section","location":"#ButterEffects.jl","page":"Home","text":"https://github.com/wookay/ButterEffects.jl","title":"ButterEffects.jl 🧈"},{"category":"section","location":"runtime_internals/","page":"Runtime Internals","text":"note: Note\ndoc fromjulia/base/runtime_internals.jl\n\nPages = [\"runtime_internals.md\"]\nDepth = 2:2\n\nPages = [\"runtime_internals.md\"]","title":"Runtime Internals"},{"category":"function","location":"runtime_internals/#Base.is_some_binding_imported","page":"Runtime Internals","text":"is_some_explicit_imported(kind) || kind == PARTITION_KIND_IMPLICIT_GLOBAL\n\n\n\n\n\n","title":"Base.is_some_binding_imported"},{"category":"function","location":"runtime_internals/#Base.is_defined_const_binding","page":"Runtime Internals","text":"(kind == PARTITION_KIND_CONST || kind == PARTITION_KIND_CONST_IMPORT || kind == PARTITION_KIND_IMPLICIT_CONST || kind == PARTITION_KIND_BACKDATED_CONST)\n\n\n\n\n\n","title":"Base.is_defined_const_binding"},{"category":"function","location":"runtime_internals/#Base.is_some_imported","page":"Runtime Internals","text":"(kind == PARTITION_KIND_IMPLICIT_GLOBAL || kind == PARTITION_KIND_IMPLICIT_CONST || kind == PARTITION_KIND_EXPLICIT || kind == PARTITION_KIND_IMPORTED)\n\n\n\n\n\n","title":"Base.is_some_imported"},{"category":"function","location":"runtime_internals/#Base.is_some_const_binding","page":"Runtime Internals","text":"(is_defined_const_binding(kind) || kind == PARTITION_KIND_UNDEF_CONST)\n\n\n\n\n\n","title":"Base.is_some_const_binding"},{"category":"function","location":"runtime_internals/#Base.is_some_implicit","page":"Runtime Internals","text":"(kind == PARTITION_KIND_IMPLICIT_GLOBAL || kind == PARTITION_KIND_IMPLICIT_CONST || kind == PARTITION_KIND_GUARD || kind == PARTITION_KIND_FAILED)\n\n\n\n\n\n","title":"Base.is_some_implicit"},{"category":"function","location":"runtime_internals/#Base.is_some_guard","page":"Runtime Internals","text":"(kind == PARTITION_KIND_GUARD || kind == PARTITION_KIND_FAILED || kind == PARTITION_KIND_UNDEF_CONST)\n\n\n\n\n\n","title":"Base.is_some_guard"},{"category":"function","location":"runtime_internals/#Base.is_some_explicit_imported","page":"Runtime Internals","text":"(kind == PARTITION_KIND_EXPLICIT || kind == PARTITION_KIND_IMPORTED)\n\n\n\n\n\n","title":"Base.is_some_explicit_imported"}]
}
